#!groovy
import groovy.json.JsonSlurper

@Library('Infrastructure') _

properties([
    parameters([
        string(name: 'PRODUCT_NAME', defaultValue: 'pre', description: 'Pre-recorded Evidence')
    ])
])

withParameterizedInfraPipeline(params.PRODUCT_NAME, 'sbox', 'sbox') {
}
// properties([
//   parameters([
//     choice(name: 'ENVIRONMENT', choices:  'sbox\ndev', description: 'Environment where code should be build and deployed')
//     choice(name: 'SUBSCRIPTION', choices: 'sbox\ndev', description: 'Subscription where code should be deployed to')
//   ])
// ])

// def product = 'pre'

// withParameterizedInfraPipeline(product, params.ENVIRONMENT, params.SUBSCRIPTION) {
// }
