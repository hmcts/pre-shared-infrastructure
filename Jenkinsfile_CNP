#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure") _

def product = "pre"

def component = "shared-infrastructure"
def product = "pre"

withParameterizedInfraPipeline(product,component,"sbox") {
    enableSlackNotifications('#pre-builds')
}